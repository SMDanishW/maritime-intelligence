# URL the browser uses to reach the FastAPI backend.
# For local dev (no Docker): http://localhost:8000
# For Docker Compose: http://localhost:8000 (backend port is host-mapped)
NEXT_PUBLIC_API_URL=http://localhost:8000
