# Copy this to secret.yaml and fill in base64-encoded values.
# secret.yaml is in .gitignore — never commit it.
#
# Encode: echo -n "your_value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: maritime-secret
  namespace: maritime
type: Opaque
data:
  POSTGRES_PASSWORD: bWFyaW5l          # "marine" — change in production
  GROQ_API_KEY: REPLACE_BASE64         # echo -n "gsk_..." | base64
